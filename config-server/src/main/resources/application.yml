server:
  port: 8888
spring:
  application:
    name: corinto-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FHOCFHOC/fhoc-config-server
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'greeting-service*'